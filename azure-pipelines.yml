trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self
  fetchDepth: 0

# -------- SonarCloud: Prepare (using Gradle scanner) --------
- task: SonarCloudPrepare@3
  inputs:
    SonarCloud: 'SonarConnection'
    organization: 'C22770139'
    scannerMode: 'gradle'
    configMode: 'file'
    extraProperties: |
      sonar.projectKey=C22770139_calculator-sonarcloud-demo
      sonar.projectName=calculator-sonarcloud-demo

# -------- Build + Test + Coverage (NO sonar task here!) --------
- task: Gradle@3
  inputs:
    gradleWrapperFile: 'gradlew'
    publishJUnitResults: true
    testResultsFiles: '**/TEST-*.xml'
    tasks: 'clean build test jacocoTestReport'

# -------- Publish Coverage to Azure DevOps --------
- task: PublishCodeCoverageResults@2
  inputs:
    summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/build/reports/jacoco/test/jacocoTestReport.xml'
    pathToSources: '$(System.DefaultWorkingDirectory)/src/main/java/'

# -------- SonarCloud Final Publish --------
- task: SonarCloudPublish@3
  inputs:
    pollingTimeoutSec: '300'
